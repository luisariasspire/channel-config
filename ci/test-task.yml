platform: linux
image_resource:
  type: docker-image
  source:
    repository: python
    tag: 3.10-alpine
inputs:
  - name: repo
run:
  path: sh
  args:
    - -exc
    - |
      set -ex
      cd repo
      ci/test-all.sh