platform: linux
image_resource:
  type: docker-image
  source:
    repository: python
    tag: 3.8-alpine
inputs:
  - name: repo
run:
  path: sh
  args:
    - -exc
    - |
      set -ex

      cd repo/lib/channel_config

      ci/test-all.sh
